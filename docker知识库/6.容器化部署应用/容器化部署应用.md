# 容器化部署应用

# 实践

## 一、构建python3 + flask镜像

### 1.启动redis数据库

flask代码，要能识别一个主机名解析，redis  >  redis机器的ip  > ip:6379

```bash
# 1.拉取一个redis镜像
docker pull redis



# 2.启动redis数据库
[root@server01 /opt/supervisor-flask]# docker run -d  --name redis  redis
```



### 2.准备源码和py模块依赖文件

实战场景是本地准备或者 git pull 都可以

```bash
#coding:utf-8
from flask import Flask
from redis import Redis

app = Flask(__name__)
redis = Redis(host='redis', port=6379)

@app.route('/')
def hello():
    count = redis.incr('hits')
    return 'linux0224 learn docker , 没毛病 , this page has been visited {} times\n'.format(count)

if __name__ == "__main__":
    app.run(host="0.0.0.0",port=8999, debug=True)



# 依赖文件，都是开发给你提供好的，直接基于这个文件装，就可以安装所有项目所需的模块
[root@docker-200 /py3-docker]#ls
requirements.txt  run-flask.py
[root@docker-200 /py3-docker]#cat requirements.txt 
flask
redis
```



### 3.准备Dockerfile并构建镜像

```bash
FROM centos:7.9.2009
RUN curl -o /etc/yum.repos.d/CentOS-Base.repo https://mirrors.aliyun.com/repo/Centos-7.repo;
RUN curl -o /etc/yum.repos.d/epel.repo http://mirrors.aliyun.com/repo/epel-7.repo;

WORKDIR /opt
ADD .  .

RUN yum makecache fast \
&& yum install python3 python3-devel python3-pip -y \
&& python3 -m pip install --upgrade pip \
&& pip3 install -r ./requirements.txt 

EXPOSE 8999
CMD ["python3","run-flask.py"]



# 构建
[root@docker-200 /py3-docker]#docker build -t t1-flask-redis  .



# 启动flask要去链接，读取redis
docker run -d  -P  --name flask-redis-app  --link=redis  t1-flask-redis
```



### 4.访问结果

![image-20241202001123172](%E5%AE%B9%E5%99%A8%E5%8C%96%E9%83%A8%E7%BD%B2%E5%BA%94%E7%94%A8.assets/image-20241202001123172.png)



## 二、使用supervisor改造nginx+flask容器

### 1.启动redis数据库

flask代码，要能识别一个主机名解析，redis  >  redis机器的ip  > ip:6379

```bash
# 1.拉取一个redis镜像
docker pull redis



# 2.启动redis数据库
[root@server01 /opt/supervisor-flask]# docker run -d  --name my-redis  redis
```



### 2.准备nginx和supervisor的配置文件、源码文件、py模块依赖文件

nginx和flask进程，在同一个容器，同时用supervisor去管理这两个进程

```bash
# 创建项目目录
[root@server01 /opt]# mkdir supervisor-flask



# nginx配置文件，前端访问后端的入口————80端口
cat > nginx_flask.conf << 'EOF'
server{
    listen 80;
    server_name _;
    location / {
        proxy_pass http://127.0.0.1:8999;
    }
}
EOF



# supervisor配置文件（本质上就是帮你去执行启动xx程序的一个工具而已）
cat > nginx_flask.ini << 'EOF'
[program:my-nginx]
command=nginx -g 'daemon off;'
autostart=true
autorestart=true
startsecs=5
redirect_stderr=true
stdout_logfile_maxbytes=20MB
stdout_logfile_backups=20
stdout_logfile=/var/log/supervisor/nginx.log

[program:my-flask]
command=python3  /opt/run-flask.py
autostart=true
autorestart=true
startsecs=5
redirect_stderr=true
stdout_logfile_maxbytes=20MB
stdout_logfile_backups=20
stdout_logfile=/var/log/supervisor/flask.log
EOF



# 源码文件
[root@docker-200 /nginx-flask]#cat run-flask.py 
#coding:utf-8
from flask import Flask
from redis import Redis

app = Flask(__name__)
redis = Redis(host='my-redis', port=6379)

@app.route('/')
def hello():
    count = redis.incr('hits')
    return 'linux0224 learn docker , 没毛病 , this page has been visited {} times\n'.format(count)

if __name__ == "__main__":
    app.run(host="0.0.0.0",port=8999, debug=True)



# python模块依赖文件
[root@docker-200 /nginx-flask]#cat requirements.txt 
flask
redis
requests
ipython



# 所有部署物料
[root@server01 /opt/supervisor-flask]# ls
nginx_flask.conf  nginx_flask.ini  requirements.txt  run-flask.py
```



### 3.编写Dockerfile并构建镜像

```bash
# dockerfile的修改，镜像中包括 nginx、supervisor、flask 三个程序
cat > Dockerfile << 'EOF'
FROM centos:7.9.2009
RUN curl -o /etc/yum.repos.d/CentOS-Base.repo https://mirrors.aliyun.com/repo/Centos-7.repo;
RUN curl -o /etc/yum.repos.d/epel.repo http://mirrors.aliyun.com/repo/epel-7.repo;

WORKDIR /opt

COPY requirements.txt /opt/
COPY run-flask.py  /opt

RUN yum makecache fast \
&& yum install python3 python3-devel python3-pip -y \
&& python3 -m pip install  --upgrade pip \
&& pip3 install  -r requirements.txt \
&& yum install supervisor nginx -y

COPY nginx_flask.ini /etc/supervisord.d/
COPY nginx_flask.conf /etc/nginx/conf.d/

RUN sed -i 's/nodaemon=false/nodaemon=true/g' /etc/supervisord.conf


EXPOSE 8999
EXPOSE 80
VOLUME /var/log/supervisor/

CMD ["/usr/bin/supervisord","-c","/etc/supervisord.conf"]

EOF

# supervisor默认启动程序是后台运行，但是docker的特性要求容器内进程必须前台运行，因此修改supervisor的配置文件

# EXPOSE 也是主动声明，运行容器后应该暴露的端口是多少



# 最终的物料
[root@docker-200 /nginx-flask]#ll
total 20
-rw-r--r-- 1 root root 816 Sep  5 23:38 Dockerfile
-rw-r--r-- 1 root root 115 Sep  5 23:25 nginx_flask.conf
-rw-r--r-- 1 root root 435 Sep  5 23:29 nginx_flask.ini
-rw-r--r-- 1 root root  29 Sep  5 23:30 requirements.txt
-rw-r--r-- 1 root root 366 Sep  5 23:25 run-flask.py



# 构建镜像
[root@server01 /opt/supervisor-flask]# docker build -t supervisor-flask-redis  .



# 启动容器（错误方式）
docker run -d  -p 80:80  --name my-supervisor-flask-redis  supervisor-flask-redis



# 观察容器内各个进程之间的关系
[root@server01 /opt/supervisor-flask]# docker exec -it 366 bash
[root@3660f680b917 opt]# 
[root@3660f680b917 opt]# ps -ef
UID         PID   PPID  C STIME TTY          TIME CMD
root          1      0  0 07:02 ?        00:00:00 /usr/bin/python /usr/bin/supervisord -c /et
root          9      1  0 07:02 ?        00:00:00 python3 /opt/run-flask.py
root         10      1  0 07:02 ?        00:00:00 nginx: master process nginx -g daemon off;
nginx        11     10  0 07:02 ?        00:00:00 nginx: worker process
nginx        12     10  0 07:02 ?        00:00:00 nginx: worker process
nginx        13     10  0 07:02 ?        00:00:00 nginx: worker process
nginx        14     10  0 07:02 ?        00:00:00 nginx: worker process
root         18      9  0 07:02 ?        00:00:07 /usr/bin/python3 /opt/run-flask.py
root         29      0  1 07:41 pts/0    00:00:00 bash
root         43     29  0 07:41 pts/0    00:00:00 ps -ef
```



### 4.访问测试

页面错误了

![image-20241203162525946](%E5%AE%B9%E5%99%A8%E5%8C%96%E9%83%A8%E7%BD%B2%E5%BA%94%E7%94%A8.assets/image-20241203162525946.png)



### 5.排查错误

```bash
# 查看容器内的日志
[root@3660f680b917 supervisor]# pwd
/var/log/supervisor
[root@3660f680b917 supervisor]# 
[root@3660f680b917 supervisor]# ls
flask.log  nginx.log  supervisord.log
[root@3660f680b917 supervisor]# 
[root@3660f680b917 supervisor]# tail -n 1000  * | grep -i error
    lambda: self._connect(), lambda error: self.disconnect(error)
    lambda: self._connect(), lambda error: self.disconnect(error)
socket.gaierror: [Errno -2] Name or service not known
    raise ConnectionError(self._error_message(e))
redis.exceptions.ConnectionError: Error -2 connecting to redis:6379. Name or service not known.



# 发现是redis的DNS解析问题，可以修改容器的启动命令、修改现有容器的 /etc/hosts 或者直接修改源代码
# 修改容器启动命令
docker run -d  -p 80:80  --name my-supervisor-flask-redis  --link=my-redis  supervisor-flask-redis

或

# 修改/etc/hosts
[root@d45b6519ad70 opt]# cat /etc/hosts
127.0.0.1	localhost
::1	localhost ip6-localhost ip6-loopback
fe00::0	ip6-localnet
ff00::0	ip6-mcastprefix
ff02::1	ip6-allnodes
ff02::2	ip6-allrouters
172.17.0.2	my-redis 398228375e54
172.17.0.3	d45b6519ad70

# 重启两个进程
[root@3660f680b917 opt]# supervisorctl -c /etc/supervisord.conf  restart all
my-flask: stopped
my-nginx: stopped
my-flask: started
my-nginx: started
[root@3660f680b917 opt]# 
[root@3660f680b917 opt]# supervisorctl -c /etc/supervisord.conf  status all 
my-flask                         RUNNING   pid 112, uptime 0:00:13
my-nginx                         RUNNING   pid 113, uptime 0:00:13

或

# 修改代码，把代码里的 redis 改为 my-redis 容器的ip
[root@3660f680b917 opt]# cat run-flask.py 
#coding:utf-8
from flask import Flask
from redis import Redis

app = Flask(__name__)
redis = Redis(host='172.17.0.2', port=6379)

@app.route('/')
def hello():
    count = redis.incr('hits')
    return 'linux0224 learn docker ，没毛病 , this page has been visited {} times\n'.format(count)

if __name__ == "__main__":
    app.run(host="0.0.0.0",port=8999, debug=True)

# 重启两个进程
[root@3660f680b917 opt]# supervisorctl -c /etc/supervisord.conf  restart all
my-flask: stopped
my-nginx: stopped
my-flask: started
my-nginx: started
[root@3660f680b917 opt]# 
[root@3660f680b917 opt]# supervisorctl -c /etc/supervisord.conf  status all 
my-flask                         RUNNING   pid 112, uptime 0:00:13
my-nginx                         RUNNING   pid 113, uptime 0:00:13
```



### 6.关于supervisor的使用

```bash
# 交互式
[root@3660f680b917 supervisor]# supervisorctl -c /etc/supervisord.conf            
my-flask                         RUNNING   pid 9, uptime 1:20:07
my-nginx                         RUNNING   pid 80, uptime 0:01:29
supervisor> 
supervisor> 



# 非交互式
[root@3660f680b917 supervisor]# supervisorctl -c /etc/supervisord.conf  stop my-nginx
my-nginx: stopped
[root@3660f680b917 supervisor]# 
[root@3660f680b917 supervisor]# supervisorctl -c /etc/supervisord.conf  status all
my-flask                         RUNNING   pid 9, uptime 1:18:31
my-nginx                         STOPPED   Dec 03 08:20 AM
[root@3660f680b917 supervisor]# 
[root@3660f680b917 supervisor]# supervisorctl -c /etc/supervisord.conf  start my-nginx
my-nginx: started
```



## 三、多阶段构建tomcat镜像

![1662368370136](%E5%AE%B9%E5%99%A8%E5%8C%96%E9%83%A8%E7%BD%B2%E5%BA%94%E7%94%A8.assets/1662368370136.png)

## 1.构建基础镜像centos7

```bash
# 1.做好基础优化，网络工具包，yum源，时间同步
FROM centos:7.9.2009

RUN rm -f /etc/yum.repos.d/*
RUN curl -o /etc/yum.repos.d/CentOS-Base.repo  https://mirrors.aliyun.com/repo/Centos-7.repo;
RUN curl -o /etc/yum.repos.d/epel.repo  http://mirrors.aliyun.com/repo/epel-7.repo;

RUN  yum install net-tools bash-completion supervisor -y \
&& yum clean all \
&& rm -rf /etc/localtime \
&& ln -s /usr/share/zoneinfo/Asia/Shanghai /etc/localtime



# 2.构建基础镜像
[root@server01 /opt/docker-learn/tomcat_web/base_centos]# ll
total 4
-rw-r--r-- 1 root root 393 Jan  4 19:24 Dockfile

# 构建镜像
docker build -t centos7_base:7.9.2009  .
```



## 2.构建jdk基础镜像

```bash
# 1.创建目录
[root@server01 /opt/docker-learn/tomcat_web]# mkdir jdk_base && cd jdk_base/



# 2.准备好环境变量文件（如果没有初始的centos7系统的环境变量，可以基于centos7镜像跑一个容器，去复制容器里的/etc/profile）
[root@server01 /opt/docker-learn/tomcat_web/jdk-base]# tail -4 profile 
export JAVA_HOME=/opt/jdk
export TOMCAT_HOME=/opt/tomcat
export PATH=$JAVA_HOME/bin:$JAVA_HOME/jre/bin:$TOMCAT_HOME/bin:$PATH
export CLASSPATH=.$CLASSPATH:$JAVA_HOME/bin:$JAVA_HOME/jre/lib:$JAVA_HOME/lib/tools.jar



# 3.准备好jdk文件
[root@server01 /opt/docker-learn/tomcat_web/jdk-base]# ll
total 190528
-rw-r--r-- 1 root root 195094741 Mar  5  2024 jdk-8u221-linux-x64.tar.gz
-rw-r--r-- 1 root root      2034 Jan  4 20:38 profile



# 4.写Dockerfile，以刚才的centos7_base:7.9.2009镜像为基础了
[root@server01 /opt/docker-learn/tomcat_web/jdk-base]# cat Dockerfile 
FROM centos7_base:7.9.2009
ADD jdk-8u221-linux-x64.tar.gz /opt
ADD profile /etc/profile

RUN ln -s /opt/jdk1.8.0_221 /opt/jdk

ENV JAVA_HOME /opt/jdk
ENV JRE_HOME $JAVA_HOME/jre
ENV CLASSPATH $JAVA_HOME/lib:$JRE_HOME/lib
ENV PATH $PATH:$JAVA_HOME/bin
# 注意这里的单独给jdk目录设置一个软连接是为了配合设置环境变量用的



# 5.构建镜像
docker build -t centos7_jdk:1.8.0  .



# 6.启动jdk容器测试，确保和如下于超老师的命令结果一样，才是正确配置好了jdk环境变量
[root@server01 /opt/docker-learn/tomcat_web/jdk-base]# docker run -it --rm centos7_jdk:1.8.0 java -version
java version "1.8.0_221"
Java(TM) SE Runtime Environment (build 1.8.0_221-b11)
Java HotSpot(TM) 64-Bit Server VM (build 25.221-b11, mixed mode)
```

7.最终目录

![image-20250104212400995](%E5%AE%B9%E5%99%A8%E5%8C%96%E9%83%A8%E7%BD%B2%E5%BA%94%E7%94%A8.assets/image-20250104212400995.png)



### 尝试使用该镜像跑一下java项目

```bash
# 发现jdk版本太低了，那我们去找一个低版本的halo博客
[root@server01 ~]# docker run --rm -v /opt/docker-learn/tomcat_web/jdk_base/jar_app/:/jar_app/  centos7_jdk:1.8.0   java -jar /jar_app/halo-2.12.0.jar
Error: A JNI error has occurred, please check your installation and try again
Exception in thread "main" java.lang.UnsupportedClassVersionError: org/springframework/boot/loader/launch/JarLauncher has been compiled by a more recent version of the Java Runtime (class file version 61.0), this version of the Java Runtime only recognizes class file versions up to 52.0
	at java.lang.ClassLoader.defineClass1(Native Method)
	at java.lang.ClassLoader.defineClass(ClassLoader.java:763)
	at java.security.SecureClassLoader.defineClass(SecureClassLoader.java:142)
	at java.net.URLClassLoader.defineClass(URLClassLoader.java:468)
	at java.net.URLClassLoader.access$100(URLClassLoader.java:74)
	at java.net.URLClassLoader$1.run(URLClassLoader.java:369)
	at java.net.URLClassLoader$1.run(URLClassLoader.java:363)
	at java.security.AccessController.doPrivileged(Native Method)
	at java.net.URLClassLoader.findClass(URLClassLoader.java:362)
	at java.lang.ClassLoader.loadClass(ClassLoader.java:424)
	at sun.misc.Launcher$AppClassLoader.loadClass(Launcher.java:349)
	at java.lang.ClassLoader.loadClass(ClassLoader.java:357)
	at sun.launcher.LauncherHelper.checkAndLoadMain(LauncherHelper.java:495)



# 更换为低版本jar包后，由于不知道halo博客会跑在哪个端口上，所以用该命令测试运行一下
[root@server01 ~]# docker run --rm -v /opt/docker-learn/tomcat_web/jdk_base/jar_app/:/jar_app/ centos7_jdk:1.8.0   java -jar /jar_app/halo-1.4.2.jar
```

<img src="%E5%AE%B9%E5%99%A8%E5%8C%96%E9%83%A8%E7%BD%B2%E5%BA%94%E7%94%A8.assets/image-20250104215618316.png" alt="image-20250104215618316" style="zoom: 50%;" />

```bash
# 最终运行成功
[root@server01 ~]# docker run -d  -p 8090:8090  -v /opt/docker-learn/tomcat_web/jdk_base/jar_app/:/jar_app/ centos7_jdk:1.8.0   java -jar /jar_app/halo-1.4.2.jar
9f51ba84d05bcf81247c19d7a11337088792f3bb8ab983fbc55a8df7b23bebb5
```



## 3.构建tomcat镜像

```bash
# 1.构建tomcat配置目录
[root@server01 /opt/docker-learn/tomcat_web]# mkdir tomcat_base && cd tomcat_base



# 2.开发Dockerfile，多阶段镜像构建
cat > Dockerfile  << 'EOF'
FROM centos7_jdk:1.8.0
ADD apache-tomcat-8.0.27.tar.gz /opt
RUN ln -s /opt/apache-tomcat-8.0.27 /opt/tomcat
EOF



# 3.构建镜像
docker build -t tomcat_base:8.0.27  . 



# 4.测试容器运行，tomcat结果
[root@server01 ~]# docker run -it --rm tomcat_base:8.0.27  bash
[root@9c50b014895e /]# 
[root@9c50b014895e /]# /opt/tomcat/bin/version.sh 
Using CATALINA_BASE:   /opt/tomcat
Using CATALINA_HOME:   /opt/tomcat
Using CATALINA_TMPDIR: /opt/tomcat/temp
Using JRE_HOME:        /opt/jdk/jre
Using CLASSPATH:       /opt/tomcat/bin/bootstrap.jar:/opt/tomcat/bin/tomcat-juli.jar
Server version: Apache Tomcat/8.0.27
Server built:   Sep 28 2015 08:17:25 UTC
Server number:  8.0.27.0
OS Name:        Linux
OS Version:     3.10.0-862.el7.x86_64
Architecture:   amd64
JVM Version:    1.8.0_221-b11
JVM Vendor:     Oracle Corporation
[root@9c50b014895e /]# 
[root@9c50b014895e /]# /opt/tomcat/bin/catalina.sh start
Using CATALINA_BASE:   /opt/tomcat
Using CATALINA_HOME:   /opt/tomcat
Using CATALINA_TMPDIR: /opt/tomcat/temp
Using JRE_HOME:        /opt/jdk/jre
Using CLASSPATH:       /opt/tomcat/bin/bootstrap.jar:/opt/tomcat/bin/tomcat-juli.jar
Tomcat started.
[root@9c50b014895e /]# 
[root@9c50b014895e /]# netstat -tunlp
Active Internet connections (only servers)
Proto Recv-Q Send-Q Local Address           Foreign Address         State       PID/Program name    
tcp6       0      0 :::8080                 :::*                    LISTEN      47/java             
tcp6       0      0 127.0.0.1:8005          :::*                    LISTEN      47/java             
tcp6       0      0 :::8009                 :::*                    LISTEN      47/java                      
```



## 4.基于tomcat镜像构建supervisor + jpress镜像

```bash
# 1.写supervisor配置文件
# 由于docker内的服务必须是前台运行，所以tomcat的启动脚本用的是/opt/tomcat/bin/catalina.sh
cat > tomcat.ini << 'EOF'
[program:my-tomcat]
command=/opt/tomcat/bin/catalina.sh run
autostart=true
autorestart=true
startsecs=5
redirect_stderr=true
stdout_logfile_maxbytes=20MB
stdout_logfile_backups=20
stdout_logfile=/var/log/supervisor/tomcat.log
EOF



# 2.准备好部署物料
# supervisor + tomcat + 业务代码（jpress.jar） = 运行环境镜像
[root@server01 /opt/docker-learn/tomcat_web/supervisor_jpress]# ll
total 67644
-rw-r--r-- 1 root root 69261961 Jan  3 12:11 jpress.war
-rw-r--r-- 1 root root      226 Jan  5 00:31 tomcat.ini



# 3.构建dockerfile
# 这里备注个火坑，前面说过了，docker内的进程，必须是前台运行，不能是守护进程，得修改supervisor配置 nodaemon=true 
FROM tomcat_base:8.0.27
COPY jpress.war /opt/tomcat/webapps/
COPY tomcat.ini /etc/supervisord.d/
RUN sed -i 's/nodaemon=false/nodaemon=true/g' /etc/supervisord.conf
EXPOSE 8080
CMD ["/usr/bin/supervisord","-c","/etc/supervisord.conf"]



# 4.构建镜像
docker build -t --no-cache supervisor_jpress:v1  .



# 5.运行测试
[root@server01 ~]# docker run -d  -p 8081:8080  --name test-jpress  supervisor_jpress:v1
0bf1646c248e486bb377b4c32a961ab4e5ecab33275afdfc8133ecbac39b5e47

[root@server01 ~]# docker ps 
CONTAINER ID   IMAGE                  COMMAND                  CREATED          STATUS          PORTS                                       NAMES
0bf1646c248e   supervisor_jpress:v1   "/usr/bin/supervisor…"   56 seconds ago   Up 54 seconds   0.0.0.0:8081->8080/tcp, :::8081->8080/tcp   test-jpress
```

![image-20250105142041964](%E5%AE%B9%E5%99%A8%E5%8C%96%E9%83%A8%E7%BD%B2%E5%BA%94%E7%94%A8.assets/image-20250105142041964.png)

> 至此确保你多阶段构建的tomcat已经可以用了，还差一个数据库。
>
> 思考
>
> 1. 数据库装在那？
> 2. 如何让业务容器，连接数据库容器？

```bash
# 让数据库跑在容器里
[root@server01 ~]# docker run --name test-mysql  -e MYSQL_ROOT_PASSWORD='linux0224'  -d -p 3306:3306  -v /var/lib/mysql:/var/lib/mysql   mysql:5.7.25 
bca69498653843f2433661267525bdf1011c21f8ff2a365fdfde7b84f20bab3c



# 在跑jpress容器时需要用--link参数
docker run -d  -p 8081:8080  --name test-jpress  --link test-mysql  supervisor_jpress:v1
```

