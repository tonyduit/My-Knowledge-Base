# 网络命令与排障

互联网普及的今天，网络变得越来越重要，相对的，工程师面对的网络问题也越来越多，做为一名程序员，相信都遇到过如下这些场景。

1. RPC调远程接口失败了，网络不通？
1. RPC调远程接口超时了，是网络导致的？
1. RPC调远程接口，调用端很慢，但服务端说它很快，如何界定问题在哪？

因此，本篇文章，带大家了解如何应对上面问题，如下：

1. 学习如何检测网络连通性，以及其它常用网络命令。
2. 学习如何使用抓包工具抓取并分析数据包。
3. 学习如何使用抓包工具判别RPC调用问题，在调用端、还是服务端，还是网络侧。

## 检测网络连通性

### DNS检测

众所周知，网络分多层，因此我们需要检测各层网络的连通性情况。

**DNS连通性检测**：

```sh
dig www.baidu.com
nslookup www.baidu.com
host www.baidu.com
```

dig使用示例：

```sh
[root@server01 ~]# dig baidu.com

; <<>> DiG 9.11.4-P2-RedHat-9.11.4-26.P2.el7_9.16 <<>> baidu.com
;; global options: +cmd
;; Got answer:
;; ->>HEADER<<- opcode: QUERY, status: NOERROR, id: 44830
;; flags: qr rd ra; QUERY: 1, ANSWER: 4, AUTHORITY: 0, ADDITIONAL: 1

;; OPT PSEUDOSECTION:
; EDNS: version: 0, flags:; udp: 1436
;; QUESTION SECTION:
;baidu.com.			IN	A

;; ANSWER SECTION:
baidu.com.		10	IN	A	124.237.177.164 # dns解析结果
baidu.com.		10	IN	A	110.242.74.102
baidu.com.		10	IN	A	111.63.65.103
baidu.com.		10	IN	A	111.63.65.247

;; Query time: 0 msec
;; SERVER: 100.100.2.136#53(100.100.2.136) # DNS服务器
;; WHEN: Fri Mar 13 12:16:34 CST 2026
;; MSG SIZE  rcvd: 102
```

可以看到根据DNS查询到的IP，以及耗时情况

#### dig 命令输出解析

1. **HEADER 部分**：显示查询的基本信息
   - `opcode`：操作码
   - `status`：响应状态
   - `id`：查询 ID
   - `flags`：标志位（如 qr, rd, ra 等）
2. **QUESTION 部分**：显示查询的问题
   - 包含查询的域名和记录类型
3. **ANSWER 部分**：查询结果
   - 包含实际的 DNS 记录信息
4. **AUTHORITY 部分**：权威域名服务器信息
   - 显示负责该域名的权威 DNS 服务器
5. **ADDITIONAL 部分**：附加信息
   - 可能包含额外的有用信息
6. **STATISTICS 部分**：查询统计
   - 显示查询耗时、服务器等信息

### IP 连通性检测

IP网络层连通情况，可以通过ping命令检测，如下：

**IP连通性检测**

```sh
ping www.baidu.com
```

ping使用示例：

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313005902870.png" alt="image-20260313005902870" style="zoom:67%;" />

可以看到icmp网络包往返耗时(rtt)情况！

### 路由情况检测

IP包是通过路由器逐步路由转发后，到达目标主机的，因此当网络慢时，可能需要检测路由情况，做为反馈给网络运营商的证据，如下：

**路由检测**

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313005947259.png" alt="image-20260313005947259" style="zoom: 67%;" />

mtr使用示例：

```sh
My traceroute  [v0.92]
example.com (192.0.2.1)                                                                2022-01-01T12:00:00+0000
Keys:  Help   Display mode   Restart statistics   Order of fields   quit
                                                                 Packets               Pings
 Host                                                        Loss%   Snt   Last   Avg  Best  Wrst StDev
 1. 192.168.1.1                                              0.0%    10    2.1   2.2   1.9   2.5   0.2
 2. 10.0.0.1                                                 0.0%    10    5.3   5.5   5.1   6.0   0.3
 3. 203.0.113.1                                              0.0%    10   15.2  15.5  15.0  16.1   0.4
 4. example.com                                              0.0%    10   20.1  20.3  20.0  21.0   0.3
```

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313010005906.png" alt="image-20260313010005906" style="zoom:67%;" />

#### mtr 关键指标解释

1. **Loss%**：丢包率，数值越高表示网络越不稳定
2. **Snt**：已发送的探测包数量
3. **Last**：最近一次探测的延迟（毫秒）
4. **Avg**：平均延迟（毫秒）
5. **Best**：最佳延迟（毫秒）
6. **Wrst**：最差延迟（毫秒）
7. **StDev**：延迟的标准差，反映网络稳定性

### tcp层端口检测

检测tcp层端口连通性，常用于检测后端进程是否存活，如下：

**端口检测**

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313010038604.png" alt="image-20260313010038604" style="zoom: 67%;" />

hping3使用示例：

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313010054970.png" alt="image-20260313010054970" style="zoom:67%;" />

注：常规端口检测使用的是tcp握手包，而这个过程由操作系统完成，进程还没参与进来，因此端口检测的rtt与进程运行性能无关！

### http接口检测

接口层面，http协议可能直接通过curl命令检测，如下：

**http协议接口检测**

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313010135860.png" alt="image-20260313010135860" style="zoom:67%;" />

注：其它协议接口，如grpc、thrift接口测试，目前还没有比较流行的工具，实在需要，可以去github上找找看。

### 一键检测

实际上，由于curl做为一个网络命令，也一定会经过DNS解析、IP层、TCP层、接口层，因此，通过curl就可以一键检测所有层连通性。

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313010209199.png" alt="image-20260313010209199" style="zoom:67%;" />

使用curl也可以一键检测各阶段耗时，如下：

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313010236277.png" alt="image-20260313010236277" style="zoom:67%;" />

- **time_namelookup**：DNS查询完成时间
- **time_connect**：tcp连接建立完成时间
- **time_starttransfer**：接收到第一个响应数据包时间。
- **time_total**：响应数据全部接收完成的时间。

## 常用网络命令

### netstat 统计tcp连接情况

通过netstat配合一些文本命令，可统计出各状态socket的数量，这有助于了解当前系统连接现状。

```sh
[root@server01 /sys/block/vda/queue]# netstat -nat | awk '/tcp/{print $6}' | sort | uniq -c
      3 ESTABLISHED
     13 LISTEN
      1 TIME_WAIT
```

需格外关注**TIME_WAIT**与**CLOSE_WAIT**状态：

- 如果TIME_WAIT过多，可考虑优化内核网络参数或使用连接池
- 如果CLOSE_WAIT过多，就需要检查程序代码中哪里出现了连接泄露，导致未关闭连接

有时，需要看看**连接的主要来源或去向**，也可通过netstat配合文本命令统计，如下：

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313010626991.png" alt="image-20260313010626991" style="zoom:50%;" />

### 查看丢包情况 - 网卡层

**ifconfig**可查看网卡层丢包情况，如下：

![image-20260313010720073](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313010720073.png)

网卡详细统计数据，可通过**ethtool -S ens33**查看。

### 查看丢包情况 - ip/tcp层

netstat可查看ip/tcp层丢包重传等情况，如下：

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313010800677.png" alt="image-20260313010800677" style="zoom: 67%;" />

详细统计数据，可通过**nstat -a**查看。

### 查看丢包情况 - 内核丢包位置

在Linux内核中，有很多种原因会导致丢包，如网卡缓冲区不够、tcp连接队列满等，因此，很多时候，需要找到**内核丢包的调用栈**，才能确定具体的丢包原因。通过bcc的**stackcount**追踪内核丢包函数**kfree_skb**即可，如下：

```sh
# 监控内核丢包函数kfree_skb的调用栈
stackcount-bpfcc  -i  1  t:skb:kfree_skb
```

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011048861.png" alt="image-20260313011048861" style="zoom:50%;" />

注：Linux还有一个工具dropwatch，也常用于跟踪丢包。

## 网络抓包工具

### tcpdump

对于线上环境的抓包，一般流程是使用tcpdump抓取网络包，然后下载后本地，使用wireshark来分析。

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011205975.png" alt="image-20260313011205975" style="zoom:50%;" />

tcpdump的抓包的最常用选项，抓包文件若太大，可考虑压缩后再下载，如下：

![image-20260313011221571](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011221571.png)

### wireshark

wireshark是一个流量分析工具，它支持大量的网络协议，如HTTP、MySQL协议，它的主界面如下：

![image-20260313011312738](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011312738.png)

wireshark支持大量的用法，本篇主要介绍如下内容：

1. 如何指定网络包解析协议
2. 如何过滤网络包
3. 如何查找网络包
4. 如何添加自定义列及排序
5. 调用端慢、服务端快的分析案例
6. 简单的统计分析功能

#### 指定解析协议

wireshark默认将80/8080端口包解析为HTTP协议，将3306端口包解析为MySQL协议，但我们mysql使用的3961端口，因此**需要告诉wireshark使用mysql协议解析3961端口流量**，如下：

![image-20260313011441492](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011441492.png)

3961端口的网络包，选择使用MySQL协议解析，如下：

![image-20260313011455043](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011455043.png)

注：如果分析HTTP流量，不是使用80端口的话，就需要选择端口以及HTTP协议。

#### 过滤包

选择完协议后，在显示过滤器中，就可以使用协议特定的字段来过滤数据包了。

使用`mysql.query contains "id=24218"`，可过滤出SQL中包含id=24218的包，如下：

![image-20260313011521577](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011521577.png)

使用`mysql.query ~ "^select"`，可正则过滤出查询类型的SQL包，如下：

![image-20260313011530876](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011530876.png)

对于wireshark不支持解析的网络协议，我们也可以通过**tcp.payload**的过滤包，如下：

![image-20260313011602398](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011602398.png)

wireshark的显示过滤器，还支持很多其它语法，常用如下：

1. **`tcp`**：过滤出tcp协议的包
2. **`tcp.port == 80`**：过滤出80端口的包
3. **`ip.addr == 192.168.61.1 and (tcp.port == 8080 or tcp.port == 3306)`**：过滤出ip地址是192.168.61.1，且端口是8080或3306的包。
4. **`mysql.query contains "id=24218"`**：过滤出包含id=24218的SQL的包。
5. **`tcp.payload ~ "select"`**：正则过滤出tcp包中包含select字符串的包。

#### 查找包

wireshark除了过滤包外，还可以查找包。使用**显示过滤器**查找包，如下：

![image-20260313011808595](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011808595.png)

也可以在包数据体中，使用**正则表达式**查找包，如下：

![image-20260313011845177](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011845177.png)

查找包与显示过滤器区别是，查找包只会定位到目标数据包，但不会过滤包列表。

#### 添加列

wireshark中默认列表头，显示了IP、协议等信息，但有时为了方便分析，需要添加一些列，如下：

![image-20260313011914666](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011914666.png)

分析tcp流量，经常会添加`tcp.time_delta`、`tcp.analysis.ack_rtt`这两列，如下：

- **`tcp.time_delta`**：当前包与同连接中上一个tcp包的时间间隔。
- **`tcp.analysis.ack_rtt`**：ack确认包与其被确认包的时间间隔。

![image-20260313011945129](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313011945129.png)

#### 包列表排序

在表头点击列，即可按此列排序包，如下，按包大小倒序排列：

![image-20260313012053150](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313012053150.png)

这一般用来看最大值与最小值。

## 调用端慢，服务端快？

### 案例：调用端慢，服务端快

有时会遇到这种情况，在全链路调用日志中发现，**调用端接口时耗时很高，但服务端处理的耗时又很短**，该如何分辨这个问题呢？是调用端问题、网络问题、还是服务端问题？

其实**全链路调用日志，记录的耗时有盲区**，如下：

1. 在调用端，请求耗时慢，可能会包含等待连接池、锁、GC等的耗时，不一定是服务端处理慢。
2. 在服务端，处理耗时快，可能会缺失请求已到达，但无空闲线程处理的耗时，不一定是服务端处理快。

因此，其实**通过网络抓包来分析耗时，是最准确的**，因为抓到请求发送的数据包，说明所有发请求的阻塞代码已执行完，请求在此刻发出去了，收包同理。

响应数据包由进程处理完请求后回复，而请求的ACK包由操作系统内核回复，因此：

1. 若回复ACK与回复响应包都慢，则是**网络慢**。
2. 若回复ACK很快，回复响应包慢，则是**服务端慢**。
3. 若ACK与响应包都很快，整体耗时依旧很高，那就是**调用端慢**。

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313012308267.png" alt="image-20260313012308267" style="zoom:50%;" />

其中ACK回复时间，对应着**tcp.analysis.ack_rtt**，而响应包回复时间，对应着**tcp.time_delta**，因此我们在调用端抓包，然后分析如下：

1.**过滤出响应包**，然后按**tcp.time_delta**倒序排序，则排在最前面的就是响应最慢的包。

![image-20260313012630213](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313012630213.png)

2.然后**选中慢包，清除掉显示过滤器，再将排序恢复为No升序**，如下：

![image-20260313012642101](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313012642101.png)

3.然后选中慢包，右键选择**对话过滤器 -> TCP**，以只看此TCP连接的数据包，如下：

![image-20260313012546405](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313012546405.png)

4.**对比tcpDelta与ack_rtt的值**，发现ack_rtt很小，而tcpDelta很大，而说明是**服务端慢，而不是网络慢**！

![image-20260313012711324](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313012711324.png)

而如果**ack_rtt**与**tcpDelta**都很小，则说明是**调用端慢**！

## 流量统计分析

wireshark也可以对网络包做一些统计分析，它预设了一些统计图表，如按**会话、端点、分组长度**等统计，不过最常用的是**I/O图表**，它可以做一些**自定义的统计分析**，如下：

![image-20260313012855577](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313012855577.png)

### IO图表

#### 统计每秒tcp收发包数量

如下，新建了一个图表，名称为**QPS**，显示过滤器为**tcp**，Y轴(Y Axis)为**Packets(包数量)**，如下：

![image-20260313012841266](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313012841266.png)

#### 统计平均响应时间

如下，新建了一个图表，名称为**Latency**，显示过滤器为**tcp.srcport < 10000**，这表示响应包，Y轴(Y Axis)为**AVG(Y Field)**，Y Field为**tcp.time_delta**，这表示响应包平均耗时，如下：

![image-20260313012950526](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313012950526.png)

## 其它抓包命令

### ngrep

ngrep是一个文本化的抓包命令，常用于包中有字符串的场景，如HTTP、MySQL等，如下：

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313013118586.png" alt="image-20260313013118586" style="zoom: 67%;" />

![image-20260313013123025](8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313013123025.png)

ngrep常用于确认请求数据的正确性，比如当代码中SQL查不到数据时，可用此确认发送给MySQL的最终SQL是否一致，避免系统中有未知的SQL改写代码，ngrep不可用时，可使用tcpdump -A代替。

### tshark

tshark是wireshark的命令行版本，常见用法如下：

<img src="8.%E7%BD%91%E7%BB%9C%E5%91%BD%E4%BB%A4%E4%B8%8E%E6%8E%92%E9%9A%9C.assets/image-20260313013153557.png" alt="image-20260313013153557" style="zoom: 50%;" />

tcpdump+wireshark是离线分析场景，tshark一般用于一些需要实时分析的场景。

## 总结

1. **ping、mtr、hping3、curl**可以用于检测各层网络连通性，以及测试网络耗时。
2. 基于**netstat**配合文本命令可统计网络socket分布情况，**ifconfig、netstat**则可以查看网络丢包情况，bcc的**stackcount**可用于定位内核丢包调用栈。
3. tcpdump可用于抓包，wireshark用于分析流量，借助**tcp.analysis.ack_rtt**与**tcp.time_delta**可用于判断是网络慢、调用端慢、还是服务端慢。
4. wireshark也可对流量做统计分析，比如包吞吐量、响应时间等。
5. 其它抓包命令，如**ngrep**常用于文本抓包，**tshark**是wireshark的命令行版本等等。





